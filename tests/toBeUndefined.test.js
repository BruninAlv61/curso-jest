let configuracion
function procesar(data) {}

test('La variable no asignada debe ser undefined', () => {
  expect(configuracion).toBeUndefined()
  expect(procesar('datos')).toBeUndefined()
})

test('', () => {})
